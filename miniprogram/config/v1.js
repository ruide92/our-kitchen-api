// Single V1 origin. Loopback is DevTools-only (urlCheck must be disabled).
// Replace with approved HTTPS origin before real device/release use. No secrets.
module.exports = { baseUrl: 'http://127.0.0.1:3101', timeoutMs: 10000 }
