# 《我们家的大食堂》微信小程序 - 完整交接文档

> 本文档用于交接给其他AI开发者（Codex/ChatGPT等）继续开发和完善本小程序。

---

## 一、项目初衷与设计理念

### 1.1 产品定位
这不是传统菜谱软件，而是一个**面向情侣和家庭的共享点餐系统**。

传统菜谱解决："红烧肉怎么做？"
本小程序解决：
- 今天谁吃饭？
- 今天吃什么？
- 她想吃什么？我想吃什么？
- 家里还有什么食材？
- 做这些菜缺什么？要买什么？
- 怎么做？
- 以前我们吃过什么？她以前点过什么？
- 什么菜我们最喜欢？

### 1.2 核心业务闭环
```
点菜 → 确认这顿吃什么 → 检查冰箱库存 → 计算所需食材
→ 减去家里已有的 → 自动生成购物清单 → 买菜 → 加入冰箱
→ 开始做饭 → 完成后确认消耗 → 扣减库存 → 双方评分
→ 获得么么哒 → 记录历史 → 下次推荐更懂这个家庭
```

### 1.3 家庭背景
- 家庭成员：我（河南人）+ 女朋友（湖南人）
- 口味偏好：都喜欢吃辣
- 饮食习惯：一顿正常一肉一素，偶尔加汤
- 女朋友特点：不吃面食、爱吃肉和苦瓜、减肥不吃米饭
- 厨具：电饭锅、炒菜锅、空气炸锅、高压锅
- 健康需求：健康食材、营养均衡

### 1.4 特色设计
- **么么哒系统**：不显示价格，显示"💋 么么哒 ×N"，代表做这道菜另一半应该奖励几个亲亲
- **动态口味属性**：普通菜显示辣度🌶，甜品显示甜度🍬，不是所有菜都固定辣度
- **家庭共享**：菜单、冰箱、购物清单、点菜历史都是家庭成员共享的
- **每周食谱**：自动生成一周科学营养搭配，早中晚三餐
- **调味品管理**：独立于冰箱，购物清单自动扣除已有调味品

---

## 二、技术栈

### 2.1 前端（微信小程序）
- 原生微信小程序开发（WXML + WXSS + JS）
- 不使用任何框架（Taro/uni-app等）
- 底部5个Tab：首页、菜单、冰箱、购物清单、我的

### 2.2 后端
- Node.js + Express
- JSON文件数据库（纯JS实现，模拟SQLite API）
- JWT认证
- 部署在Render免费托管平台

### 2.3 为什么用JSON文件数据库
- 最初尝试better-sqlite3，但用户电脑缺少Visual Studio C++编译工具，安装失败
- 改用纯JS实现的JsonDatabase类，模拟SQLite的prepare/get/all/run API
- 数据存在database.json文件中
- 适合小规模家庭使用，未来可迁移到真正的数据库

---

## 三、项目结构

### 3.1 小程序前端
路径：`C:\Users\zhang\Desktop\我们的小厨房-原生小程序`

```
├── app.js              # 全局逻辑（登录、家庭管理）
├── app.json            # 小程序配置（页面、TabBar）
├── app.wxss            # 全局样式
├── project.config.json # 项目配置
├── sitemap.json        # 站点地图
├── utils/
│   ├── api.js          # API封装（所有后端接口调用）
│   └── util.js         # 工具函数（时间格式化、提示等）
└── pages/
    ├── index/          # 首页（家庭身份+搜索+快捷功能+每周食谱）
    ├── menu/           # 菜单页（左侧分类+右侧菜品列表）
    ├── detail/         # 菜品详情页（大图+信息+食材+步骤+操作）
    ├── fridge/         # 冰箱页（库存管理）
    ├── shopping/       # 购物清单页
    ├── mine/           # 我的页面（登录+统计+功能菜单）
    ├── family/         # 家庭管理页（成员、邀请码）
    ├── favorites/      # 收藏页
    ├── ratings/        # 评分/点赞菜谱页
    ├── orders/         # 点菜历史页
    ├── today-menu/     # 今日菜单页
    ├── random/         # 随机点餐页（今天吃什么）
    ├── add-recipe/     # 添加/编辑菜谱页
    ├── ai-import/      # AI菜谱导入页（KRP协议）
    ├── recycle/        # 回收站页
    └── seasoning/      # 调味品管理页
```

### 3.2 后端
路径：`C:\Users\zhang\Desktop\我们的小厨房-后端`

```
├── server.js           # 入口文件（端口3001）
├── package.json        # 依赖配置
├── database.js         # JSON文件数据库实现（模拟SQLite API）
├── database.json       # 数据库文件（含45道菜品数据）
├── Procfile            # Render部署配置
├── .gitignore          # Git忽略配置
├── init-dishes.js      # 初始菜品导入脚本
├── init-more-dishes.js # 补充菜品导入脚本
├── test-api.js         # API测试脚本
├── middleware/
│   └── auth.js         # JWT认证中间件
└── routes/
    ├── auth.js         # 登录/用户信息
    ├── dishes.js       # 菜品列表/详情/分类/收藏/评分
    ├── family.js       # 家庭创建/加入/成员管理
    ├── fridge.js       # 冰箱库存管理
    ├── shopping.js     # 购物清单管理
    ├── menu.js         # 每周食谱
    ├── condiments.js   # 调味品管理
    ├── favorites.js    # 收藏管理
    └── orders.js       # 点菜历史
```

---

## 四、核心功能说明

### 4.1 登录与家庭体系
- 首次进入自动调用wx.login()获取code
- 后端用code的MD5模拟openid（因为没有AppSecret）
- 自动创建用户，自动创建默认家庭"我们家的大食堂"
- 用户可设置微信头像和昵称
- 家庭角色：OWNER（所有者）、ADMIN（管理员）、MEMBER（普通成员）
- 支持邀请码邀请家人加入

### 4.2 首页
- 顶部：双人头像 + 厨房名称 + 问候语
- 搜索框：搜索菜谱、食材、菜系
- 快捷功能面板：今天吃什么（随机）、看冰箱做菜、喜欢吃的菜、一人菜
- 每周食谱：7天时间轴，每天早中晚三餐，营养标签，可换一周

### 4.3 菜单页
- 左侧窄分类栏：推荐、热菜、凉菜、汤羹、荤菜、素菜、主食、早餐、甜品等
- 右侧紧凑菜品卡片列表：图片、菜名、标签、时间、辣度、健康度
- 支持按分类筛选、关键词搜索
- 点击进入菜品详情

### 4.4 菜品详情页
- 顶部4:3大图
- 菜名、标签、制作时间、难度、适合人数、厨具
- 动态口味属性：普通菜显示辣度🌶，甜品显示甜度🍬
- 健康程度（1-5星）、营养特点
- 么么哒奖励（💋×N）
- 主料、配菜、调料（结构化数据）
- 详细制作步骤（每步含操作、时间、火力、判断状态）
- 烹饪技巧、防失败提示、替代食材
- 底部操作：加入菜单、收藏、我想吃、开始做饭、改成我家的做法

### 4.5 冰箱管理
- 分类：冷藏、冷冻、常温、调料
- 每项：名称、数量、单位、位置、购买时间、保质期
- 支持增删改查
- 根据库存推荐能做的菜
- 优先消耗提醒（快过期的食材）

### 4.6 购物清单
- 从菜品自动生成所需食材
- 合并重复食材
- 自动扣除冰箱已有库存
- 自动扣除调味品（盐、酱油等默认已有）
- 支持单独勾选、全选、标记已购买、修改数量、删除
- 购买完成后一键加入冰箱

### 4.7 每周食谱
- 自动生成7天营养均衡食谱
- 每天：早餐2道、午餐1荤1素、晚餐1荤1素1汤
- 营养主题每天不同（优质蛋白+膳食纤维、豆制品+绿叶蔬菜等）
- 可手动换一周重新生成
- 可把菜品加入今日菜单

### 4.8 评分与收藏
- 每道菜可独立评分（1-5星）
- 支持取消评分（再次点击同一星数取消）
- 收藏功能，分别记录我的收藏、她的收藏
- 家庭喜爱度 = 所有成员评分的平均值

### 4.9 么么哒系统
- 每道菜有建议么么哒数（💋×N）
- 做完菜后可奖励么么哒
- 么么哒不是评分，是独立的情感互动
- 未来可做么么哒排行榜

### 4.10 AI菜谱导入（KRP协议）
- 正式名称：Kitchen Recipe Import Protocol（小厨房菜谱包协议）
- 流程：复制AI Skill → 发给豆包/ChatGPT → AI生成KRP → 粘贴回来 → 解析校验 → 预览 → 入库
- KRP格式：`<KITCHEN_RECIPE_PACK>{...}</KITCHEN_RECIPE_PACK>`
- 支持字段校验、缺失提示、修复建议

### 4.11 调味品管理
- 独立于冰箱管理
- 预置常用调味品：盐、酱油、醋、糖、料酒、豆瓣酱等
- 购物清单生成时自动扣除已有调味品
- 支持自定义添加

---

## 五、后端API说明

### 5.1 基础信息
- 线上API地址：`https://our-kitchen-api-6vk6.onrender.com/api`
- 本地API地址：`http://localhost:3001/api`
- 认证方式：Bearer Token（JWT）
- 数据格式：JSON

### 5.2 API列表

#### 认证相关
- `POST /auth/login` - 微信登录（参数：code, nickname, avatar）
- `GET /auth/info` - 获取用户信息（需登录）
- `PUT /auth/info` - 更新用户信息（需登录，参数：nickname, avatar）

#### 菜品相关
- `GET /dishes` - 菜品列表（支持category, keyword, page, pageSize等参数）
- `GET /dishes/:id` - 菜品详情
- `GET /dishes/hot/list` - 热门菜品
- `GET /dishes/categories/list` - 分类列表
- `POST /dishes/:id/rate` - 评分（需登录，参数：rating）
- `POST /dishes/:id/favorite` - 收藏/取消收藏（需登录）
- `GET /dishes/my-rated/list` - 我的评分列表（需登录）

#### 家庭相关
- `POST /family/create` - 创建家庭（需登录，参数：name）
- `POST /family/join` - 加入家庭（需登录，参数：code）
- `GET /family/mine` - 我的家庭（需登录）
- `GET /family/members` - 家庭成员列表（需登录）
- `PUT /family/update` - 修改家庭信息（需登录）

#### 冰箱相关
- `GET /fridge` - 冰箱库存列表（需登录）
- `POST /fridge` - 添加食材（需登录，参数：name, amount, unit, category, location）
- `PUT /fridge/:id` - 修改食材（需登录）
- `DELETE /fridge/:id` - 删除食材（需登录）

#### 购物清单相关
- `GET /shopping` - 购物清单（需登录）
- `POST /shopping` - 添加购物项（需登录）
- `POST /shopping/generate` - 从菜品生成购物清单（需登录，参数：dishIds）
- `PUT /shopping/:id` - 修改购物项（需登录）
- `DELETE /shopping/:id` - 删除购物项（需登录）
- `POST /shopping/:id/toggle` - 勾选/取消勾选（需登录）
- `POST /shopping/check-all` - 全选/取消全选（需登录）
- `POST /shopping/move-to-fridge` - 购买完成加入冰箱（需登录）

#### 每周食谱相关
- `GET /menu` - 获取每周食谱（需登录，为空时自动生成）
- `POST /menu` - 添加菜品到周食谱（需登录）
- `DELETE /menu/:id` - 从周食谱移除（需登录）
- `POST /menu/generate` - 重新生成每周食谱（需登录）

#### 调味品相关
- `GET /condiments` - 调味品列表（需登录）
- `POST /condiments` - 添加调味品（需登录）
- `PUT /condiments/:id` - 修改调味品（需登录）
- `DELETE /condiments/:id` - 删除调味品（需登录）
- `POST /condiments/batch-add` - 批量添加预置调味品（需登录）

#### 收藏相关
- `GET /favorites` - 收藏列表（需登录）
- `GET /favorites/check/:dishId` - 检查是否收藏（需登录）

#### 点菜相关
- `POST /orders` - 创建点菜（需登录，参数：dishId）
- `GET /orders` - 点菜列表（需登录）
- `GET /orders/history` - 点菜历史（需登录）
- `PUT /orders/:id` - 更新点菜状态（需登录）
- `DELETE /orders/:id` - 删除点菜（需登录）

---

## 六、数据库结构

### 6.1 主要表
- `users` - 用户表（id, openid, nickname, avatar, created_at）
- `families` - 家庭表（id, name, invite_code, created_by, created_at）
- `family_members` - 家庭成员表（id, family_id, user_id, role, joined_at）
- `dishes` - 菜品表（id, name, description, category, cuisine, cookware, spiciness, sweetness, healthiness, difficulty, cook_time, servings, kiss_reward, image_url, ingredients, steps, tips, tags, is_custom, is_hidden, is_lazy, created_at）
- `weekly_menu` - 每周食谱表（id, family_id, dish_id, week_day, meal_type, added_by）
- `fridge_items` - 冰箱库存表（id, family_id, name, amount, unit, category, location, purchase_date, expiry_date, note）
- `shopping_items` - 购物清单表（id, family_id, name, amount, unit, category, is_checked, added_by, created_at）
- `condiments` - 调味品表（id, family_id, name, amount, unit, created_at）
- `favorites` - 收藏表（id, user_id, dish_id, created_at）
- `dish_ratings` - 评分表（id, user_id, dish_id, rating, created_at）
- `orders` - 点菜表（id, family_id, user_id, dish_id, status, note, created_at）
- `today_menu` - 今日菜单表（id, family_id, dish_id, meal_type, added_by, created_at）

### 6.2 菜品数据字段说明
- `ingredients` - JSON数组，每项含name, amount, unit, type(main/side/seasoning/garnish), required
- `steps` - JSON数组，每项含step, operation, time, heat, tip
- `tips` - JSON数组，烹饪技巧
- `tags` - JSON数组，标签
- `cookware` - JSON数组，所需厨具

---

## 七、设计规范

### 7.1 设计语言
- **整体风格**：温暖、清爽、有生活感，不做成企业后台，不做冷冰冰的工具App
- **背景色**：暖米白 #FFF9F0
- **卡片背景**：白色 #FFFFFF
- **功能主色**：绿色 #7CB342（按钮、主要操作）
- **强调色**：橙色 #FF9800（标签、次要操作）
- **情感色**：粉色 #E91E63（么么哒、收藏、爱心）
- **主文字**：深棕 #3E2723
- **次文字**：灰色 #8D6E63
- **边框**：极淡灰 #F5F5F5
- **圆角**：16-18px（卡片），24px（按钮）
- **阴影**：极淡阴影，不厚重

### 7.2 布局原则
- 手机端优先，按iPhone 13尺寸（390×844）设计
- 节省空间，菜品卡片不要太大，字不要太大
- 首页首屏要能看到更多信息，不要下滑才能看到关键内容
- 菜单页用左侧窄分类栏+右侧紧凑列表
- 不要横向滚动，不要复杂的滑动操作

### 7.3 交互原则
- 一次动作尽量只要求一次决策
- 普通操作直接执行+可撤销，不要每次弹确认
- 危险操作（永久删除、清空）才二次确认
- 登录成功后自动跳转到首页
- 所有操作要有反馈（成功/失败提示）

---

## 八、如何运行和调试

### 8.1 后端本地运行
```bash
cd C:\Users\zhang\Desktop\我们的小厨房-后端
npm install
node server.js
```
后端启动在 http://localhost:3001

### 8.2 小程序本地调试
1. 打开微信开发者工具
2. 导入项目：`C:\Users\zhang\Desktop\我们的小厨房-原生小程序`
3. AppID：`wxbd67ce4437e3ea3b`（正式）或测试号
4. 如果要连本地后端，修改 `utils/api.js` 中的 BASE_URL 为 `http://localhost:3001/api`
5. 点击编译即可预览

### 8.3 API测试
```bash
cd C:\Users\zhang\Desktop\我们的小厨房-后端
node test-api.js
```

---

## 九、如何上传到微信开发者工具并发布

### 9.1 上传代码到微信平台
**方法一：命令行上传（推荐）**
```bash
# 微信开发者工具CLI路径
"D:\微信web开发者工具\cli.bat" upload --project "C:\Users\zhang\Desktop\我们的小厨房-原生小程序" --version "1.0.0" --desc "版本描述"
```

**方法二：微信开发者工具界面上传**
1. 打开微信开发者工具，导入项目
2. 点击右上角「上传」按钮
3. 填写版本号和版本描述
4. 点击上传

### 9.2 提交审核
1. 登录微信公众平台：https://mp.weixin.qq.com
2. 进入「管理」→「版本管理」
3. 在「开发版本」中找到刚上传的版本
4. 点击「提交审核」
5. 填写审核信息：
   - 版本描述：简要说明本次更新内容
   - 测试账号：选择「无需登录账号即可使用」（因为我们是静默登录）
   - 用户隐私保护指引：已配置的话选择「采集用户隐私」
6. 提交审核，等待审核结果（通常1-7天）

### 9.3 发布上线
1. 审核通过后，在「版本管理」→「审核版本」中找到该版本
2. 点击「发布」
3. 确认发布，小程序正式上线
4. 用户可以在微信中搜索小程序名称找到并使用

### 9.4 体验版测试
1. 在「开发版本」中点击「选为体验版」
2. 用微信扫码即可体验最新版本
3. 体验版不需要审核，上传后立即可用
4. 建议先在体验版充分测试，再提交审核

### 9.5 重要配置
- **request合法域名**：需要在微信公众平台「开发」→「开发管理」→「开发设置」→「服务器域名」中添加 `https://our-kitchen-api-6vk6.onrender.com`
- **AppID**：`wxbd67ce4437e3ea3b`
- **小程序名称**：我们家的大食堂
- **主体类型**：个人
- **服务类目**：工具→备忘录

---

## 十、已知问题和待办事项

### 10.1 已修复的问题
- ✅ 后端parseWhere函数bug（params.shift()在filter内部多次调用，导致所有带WHERE的查询失败）
- ✅ 登录无反应（后端bug导致）
- ✅ 每周食谱为空（用户首次登录没有家庭，已改为自动创建家庭+自动生成食谱）
- ✅ 菜品图片不显示（后端返回image_url，前端用image，已统一为image字段）
- ✅ 版本号显示1.0.0（已更新为1.3.1）

### 10.2 待完善功能
- ⏳ 微信登录真实AppSecret配置（当前用code的MD5模拟openid，不同设备可能生成不同openid）
- ⏳ 手机号绑定功能（个人主体可能不支持）
- ⏳ 做饭大字模式（一次显示一个步骤，带计时）
- ⏳ 食材保质期提醒
- ⏳ 本周菜单提前安排功能
- ⏳ 操作日志界面
- ⏳ 宝宝辅食专区
- ⏳ 老人餐专区
- ⏳ 多厨房切换
- ⏳ 数据导入/导出
- ⏳ 图片AI生成
- ⏳ 分享广场（未来开源后）
- ⏳ TabBar图标（当前只有文字没有图标）

### 10.3 已知限制
- 个人主体小程序不支持web-view组件
- 个人主体小程序可能不支持手机号获取
- Render免费实例15分钟无请求后休眠，第一次请求需要50秒+启动
- JSON文件数据库不支持高并发，适合小规模家庭使用
- 当前用code的MD5模拟openid，不是真正的微信登录

### 10.4 需要注意的坑
- WXML中不支持箭头函数、直接调用JS方法/getter，所有计算必须在JS的data中预计算或使用wxs
- 微信小程序获取用户头像昵称需使用button open-type="chooseAvatar"和input type="nickname"组件，不能直接调用wx.getUserInfo（已废弃）
- 所有JSON文件必须使用UTF-8无BOM编码，否则微信开发者工具编译报错
- PowerShell中使用curl.exe发送POST请求时，JSON双引号会被去掉导致解析失败，建议用Node.js的http模块测试API
- 小程序正式版必须配置request合法域名，否则无法访问后端
- 体验版和开发版可以在微信开发者工具中勾选「不校验合法域名」进行调试

---

## 十一、关键ID和配置

### 11.1 小程序信息
- 小程序名称：我们家的大食堂
- AppID：`wxbd67ce4437e3ea3b`
- 主体类型：个人
- 服务类目：工具→备忘录

### 11.2 后端信息
- 线上API地址：`https://our-kitchen-api-6vk6.onrender.com/api`
- 本地API地址：`http://localhost:3001/api`
- Render服务ID：`srv-dacf3iifngtc73d8c380`
- GitHub仓库：`https://github.com/ruide92/our-kitchen-api`
- JWT密钥：`our-kitchen-secret-key-2024`

### 11.3 本地路径
- 小程序前端：`C:\Users\zhang\Desktop\我们的小厨房-原生小程序`
- 后端：`C:\Users\zhang\Desktop\我们的小厨房-后端`
- 微信开发者工具：`D:\微信web开发者工具`
- 微信开发者工具CLI：`D:\微信web开发者工具\cli.bat`

### 11.4 菜品数据
- 当前菜品数量：45道
- 包含分类：荤菜、素菜、热菜、凉菜、汤品、主食、早餐、甜品
- 包含菜系：河南菜、湖南菜、江西小炒、家常菜、懒人菜
- 包含厨具：炒锅、电饭锅、高压锅、空气炸锅

---

## 十二、继续开发建议

### 12.1 优先级排序
1. **P0（必须）**：确保核心功能稳定可用（登录、菜单、详情、冰箱、购物清单）
2. **P1（重要）**：完善做饭模式、评分取消、购物清单全选等交互细节
3. **P2（体验）**：添加TabBar图标、优化UI细节、增加动效
4. **P3（扩展）**：每周菜单提前安排、保质期提醒、数据导出
5. **P4（未来）**：分享广场、多厨房切换、宝宝辅食、老人餐

### 12.2 代码质量建议
- 组件化：抽出公共组件（RecipeCard、CategorySidebar、TasteBadge等）
- 状态管理：考虑用全局状态管理，避免每个页面单独处理登录状态
- 错误处理：统一API错误处理，给用户友好提示
- 代码注释：关键逻辑添加注释，方便后续维护
- 版本管理：小程序前端代码也上传到GitHub，方便版本管理和备份

### 12.3 后端优化建议
- 迁移到真正的数据库（SQLite/PostgreSQL）
- 添加数据备份机制
- 优化查询性能（添加索引）
- 添加API日志和监控
- 考虑使用Redis缓存热门数据

---

## 十三、联系方式和资源

- 微信公众平台：https://mp.weixin.qq.com
- 微信开发者工具下载：https://developers.weixin.qq.com/miniprogram/dev/devtools/download.html
- 微信小程序开发文档：https://developers.weixin.qq.com/miniprogram/dev/framework/
- Render控制台：https://dashboard.render.com
- GitHub后端仓库：https://github.com/ruide92/our-kitchen-api

---

> **文档版本**：v1.0
> **最后更新**：2026-09-03
> **小程序当前版本**：1.3.1
> **文档目的**：交接给其他AI开发者继续开发和完善本小程序
